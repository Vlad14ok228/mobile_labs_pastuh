package com.vovan.lab7.data.entity

// Це data class, який слугує моделлю для зберігання однієї пари: "Питання - Відповідь".
// Уся вікторина буде списком із цих об'єктів (List<TextPair>).
data class TextPair(

    val question: String, // 👈 Поле для зберігання згенерованого ШІ-питання (наприклад, "Яка столиця Франції?").

    val answer: String // 👈 Поле для зберігання згенерованої ШІ-відповіді (наприклад, "Париж").

    // Після того, як Gemini API згенерує текст, цей клас допоможе нам організовано його зберігати
    // у зручному форматі для відображення у LazyColumn.
)