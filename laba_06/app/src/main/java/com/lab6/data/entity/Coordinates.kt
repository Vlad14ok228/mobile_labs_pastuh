package com.lab6.data.entity

// Це data class, який слугує моделлю для парсингу (читання) частини JSON-відповіді.
// Він відповідає за зберігання географічних даних.
data class Coordinates(

    val lat: Double, // 👈 Широта (latitude). Число з плаваючою комою.

    val lon: Double  // 👈 Довгота (longitude). Число з плаваючою комою.

    // Цей клас відповідає JSON-об'єкту, який виглядає приблизно так:
    // "coord": { "lon": 30.52, "lat": 50.45 }
)
